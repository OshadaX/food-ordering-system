export default function Footer() {
  return (
    <footer className="border-top bg-white mt-auto">
      <div className="container py-3 text-muted small d-flex justify-content-between flex-wrap gap-2">
        <span>Food Ordering System demo workspace</span>
        <span>Tracking module ready for testing</span>
      </div>
    </footer>
  )
}
